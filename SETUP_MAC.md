# ClosetScan — macOS / iPhone setup

This project was authored on Windows. Build and run only on a Mac with Xcode + a LiDAR iPhone.

```bash
# From this directory on your Mac:
open ClosetScan.xcodeproj
```

Then set Signing Team and run on a physical LiDAR device.
